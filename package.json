{
  "name": "nanolytics",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --outfile dist/client.js src/client.ts --minify",
    "build:dev": "bun build --outfile dist/client.js src/client.ts --watch",
    "dev": "bun run src/index.ts --hot"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.0.5"
  }
}
